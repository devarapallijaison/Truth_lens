{
  "name": "truthlens",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace truthlens-client",
    "dev:server": "npm run dev --workspace truthlens-server",
    "build": "npm run build --workspace truthlens-client",
    "start": "npm run start --workspace truthlens-server"
  }
}
